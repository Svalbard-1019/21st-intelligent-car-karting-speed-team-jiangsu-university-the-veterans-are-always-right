# Final 分支交接文档

> 更新日期：2026-07-27
> 项目目录：`D:\kadingkuaipao\imitation\lsddd\kart_508\kart_508`
> 仓库：`Svalbard-1019/kart_508`
> 当前分支：`final`

## 0. 交接摘要（先读这一节）

这是当前 `kart_508` 的权威交接文件。仓库现阶段的主线不是 SCC8660 视觉跟随，而是：

```text
遥控打点/路线记录
    -> 编码器 + IMU/GPS 里程定位
    -> Portion1/Portion3 路线跟踪
    -> 停车入口对齐
    -> 实录/规划倒车
    -> 主动制动停车
```

本次交接更新前的 `final` 基线：

```text
8cfe516 docs: refresh final handoff for portion1 tuning
```

当前最新功能代码提交：

```text
8434365 fix: adapt portion1 reverse lookahead to speed
```

### 0.1 累计已经写入仓库的主要能力

以下能力已经进入 Git 历史，不只是方案文档：

- 前轮角度电机闭环、转向方向/限幅和高速转向响应修正。
- 后轮编码器速度闭环、HIP4082 四 PWM 驱动、启动 PWM 下限与全局主动制动。
- 遥控器独立驾驶、遥控打点、路线保存和发车模式切换。
- Portion1 路线记录、里程计跟踪、弯道提前准备、弯道速度分档、档位滞回和加减速斜率限制。
- Portion3 路线记录/返程、GPS/IMU 辅助和相关串口诊断。
- 停车点记录、停车入口判定、局部 SE(2) 路线对齐、实录倒车路线和兜底倒车路径。
- Portion1 倒车速度菜单化、终点精调速度、倒车启动 PWM 下限和按速度自适应前视。
- 串口 `AUTO` 诊断链路，可观察速度请求、实际速度、PWM、转向、倒车状态、路线索引、终点误差和当前前视。
- 9 个 Python 测试文件，覆盖最终集成、后轮接口、电机映射、里程计、速度规划、倒车速度/前视、主动制动等关键契约。

### 0.2 已验证到什么程度

| 层级 | 当前状态 |
|---|---|
| Git | `final` 已推送，功能提交和交接提交均在 `origin/final` |
| 主机测试 | 2026-07-26 运行 47 项，全部通过 |
| 静态契约 | 已检查关键调用、串口字段、速度规划和纯 C 计算模块 |
| ADS/TASKING | 最新 `p1spd6` 尚未完成 Clean/Build/Link |
| TC264 烧录 | 最新 `p1spd6` 尚未确认烧录 |
| 实车 | 旧版本有测试数据；最新自适应倒车前视尚未完成重复实车验证 |

因此目前只能说“源码和主机测试完成”，不能说“最新固件已经实车验收完成”。

### 0.3 明确没有写入/没有完成的部分

- **SCC8660 单目视觉跟随代码没有集成到这个仓库。** 当前没有 `vision_pipeline`、颜色自适应、目标检测/跟踪、视觉距离模型或视觉跟随控制器。
- 本地存在未跟踪研究文档 `docs/kms-visual-follow-return-research-2026-07-16.md`，但它尚未提交，且只是研究/设计，不是可烧录实现。
- 最新 `p1spd6` 没有完成 ADS 编译、TC264 烧录和连续实车验收。
- Portion3 保存/重启后疑似少一个路线点的问题尚未修复。
- Portion1 偶发少一个路线点的根因尚未定位。
- 停车入口姿态重复性、最终停车距离和高速倒车安全范围尚未收敛。
- 倒车转向增益、转向命令变化率和底层转向 PID 尚未基于最新日志继续优化。

### 0.4 接手优先级

| 优先级 | 事项 | 完成判据 |
|---|---|---|
| P0 | ADS Clean/Build 最新 `final` | 0 编译/链接错误，无 RAM/Flash 冲突 |
| P0 | 烧录并核对固件标记 | 串口出现 `cfg=p1spd6` |
| P0 | 低速架空/空场安全检查 | 转向、前进、倒车、制动方向全部正确 |
| P1 | `-10` 倒车连续测试至少 5 次 | 保存完整日志并统计最终位置/航向误差 |
| P1 | 根据 `revCmd10` 与 `actS10` 分层定位摆动 | 能区分上层路径控制与底层转向闭环问题 |
| P1 | 路线点保存一致性测试 | 记录前、保存后、重启后长度及末三点完全一致 |
| P2 | 收紧最终停车距离 | 在无超时、无撞击且入口姿态稳定后由 12 cm 逐步调整 |
| P2 | 视觉跟随单独立项 | 先提交设计与主机算法测试，再接入 TC264 主循环 |

## 1. 当前权威版本

2026-07-26 本轮功能代码的最终提交为：

```text
8434365 fix: adapt portion1 reverse lookahead to speed
```

该提交已推送到 `origin/final`，推送后本地与远程 SHA 已核对一致。

本轮相关提交：

```text
a017bac fix: stabilize portion1 auto speed planning
ffcea44 fix: refine portion1 speed and reverse control
ded1ac0 fix: tune portion1 speed and menu reverse control
18bb50c docs: design adaptive reverse lookahead
ed95d35 docs: plan adaptive reverse lookahead
8434365 fix: adapt portion1 reverse lookahead to speed
```

串口固件识别标记：

```text
cfg=p1spd6
```

烧录后如果日志里不是 `cfg=p1spd6`，说明 ADS 仍在使用旧 ELF，当前测试数据不能用于判断本版效果。

## 2. 本轮已完成的工作

### 2.1 Portion1 前进速度规划

解决的目标是减少直线阶段“加速一段、滑行一下、再继续加速”的速度命令波动，并缩短全程时间。

当前最终参数：

| 项目 | 当前值 |
|---|---:|
| 未来转弯检测窗口 | 2.40 m |
| 小角度打点抖动死区 | 2.5° |
| 加速度限制 | 3.5 m/s² |
| 减速度限制 | 4.0 m/s² |
| 累计小弯速度比例 | 0.80 |
| 累计中弯速度比例 | 0.70 |
| 累计大弯速度比例 | 0.60 |
| 发卡弯保护比例 | 0.45 |

已经加入：

- 按物理距离统计未来弯道，不再只依赖固定点数。
- 过滤小于 2.5° 的路线打点抖动。
- 弯道档位滞回，减少速度比例在相邻档位间反复切换。
- 加速和减速使用不同变化率。
- 科目一与科目三的速度规划参数继续隔离；本次没有修改 portion3 的参数。

### 2.2 停车入口减速

停车入口减速距离由：

```text
1.20 m -> 2.00 m
```

目的：让车辆在进入自动倒车前更早稳定速度和姿态，降低倒车路线整体平移或旋转的概率。

### 2.3 倒车速度改为菜单控制

自动倒车不再使用写死的巡航速度。以下三条 portion1 自动倒车路径都读取菜单 `Daoche_Speed`：

- 实录教学倒车路线。
- 自动规划倒车路线。
- 目标点兜底倒车。

菜单范围：

```text
-2 ～ -40
```

换算关系：

```text
菜单值 × 0.1 = m/s
-4  = 0.4 m/s
-10 = 1.0 m/s
-40 = 4.0 m/s
```

Flash 默认值为 `-4`。旧 Flash 值如果超出新范围，上电后回退到 `-4`。

终点精调速度规则：

```text
fine = 菜单速度 × 0.75
fine 最快限制为 -3（约 0.3 m/s）
```

例如：

| 菜单值 | 巡航速度 | 终点精调速度 |
|---:|---:|---:|
| -2 | 0.2 m/s | 0.15 m/s |
| -4 | 0.4 m/s | 0.3 m/s |
| -10 | 1.0 m/s | 0.3 m/s |
| -40 | 4.0 m/s | 0.3 m/s |

### 2.4 倒车速度自适应前视

发现菜单设为 `-10`（约 1.0 m/s）时，车辆虽然最终能入库，但会左右大幅纠正。

根因：

- 原实录倒车跟踪固定使用 0.35 m 前视。
- 轴距为 0.724 m，转向增益为 1.40。
- 速度升高后仍使用短前视，小航向误差会产生很大的目标转角。
- 车辆尚未完成上一次修正就越过路线，随后控制器向另一侧大幅纠正，形成摆动。

现在实录倒车路径按速度自适应前视距离：

```text
速度 <= 0.4 m/s：前视 0.35 m
速度 > 0.4 m/s：每增加 1.0 m/s，前视增加 0.60 m
最大前视：1.20 m
```

典型值：

| 倒车速度 | 前视距离 |
|---:|---:|
| 0.4 m/s | 0.35 m |
| 1.0 m/s | 0.71 m |
| 高速 | 最高 1.20 m |

终点进入精调速度后，前视会自动恢复到 0.35 m，以保留最终入库精度。

本次只改自适应前视，没有同时调整：

- 倒车转向增益 `1.40`。
- 转向命令变化速率。
- 最大转向命令 `35°`。
- 底层转向 PID。

这样下一次实车测试能单独判断“增大高速前视”是否解决摆动。

### 2.5 倒车低速驱动

之前倒车精调时 PWM 容易锁在较高启动下限。本轮之前已把倒车 PWM 下限改为只在启动阶段生效：

- 静止起步时保留足够 PWM 克服静摩擦。
- 已经移动后允许 PID 把 PWM 降到更低。
- 精调阶段不再长期锁死在约 `-1800`。

该逻辑继续保留，本次自适应前视没有修改后轮 PID。

### 2.6 串口诊断

portion1 的 `AUTO` 日志当前包含：

```text
cfg=p1spd6
pwm
turn10
turnLv
req100
cmd100
revSt
revPlan
revIdx
revLen
revD100
revYaw10
revCmd10
revLd100
```

重点字段：

| 字段 | 含义 |
|---|---|
| `req100` | 速度规划原始请求 |
| `cmd100` | 加减速限制后的命令 |
| `tgt100` | 后轮目标速度 |
| `act100` | 后轮实际速度 |
| `revCmd10` | 倒车上层转向命令 |
| `actS10` | 实际前轮角度 |
| `revLd100` | 实录倒车当前前视距离，单位 cm |
| `revD100` | 距倒车最终目标距离，单位 cm |
| `revYaw10` | 倒车最终航向误差 |

1.0 m/s 实录倒车巡航时，`revLd100` 应约为 `71`；进入终点精调后应回到约 `35`。

## 3. 本轮实车数据结论

此前三组倒车数据中：

- 3.5 速度测试正常结束，最终约 `9 cm / 0.8°`。
- 3.8 速度测试正常结束，最终约 `12 cm / 1.4°`。
- 4.2 速度测试不是正常到位停车：在路线约 `19/21` 点、距离终点约 62 cm 时停止移动，PWM 已增加但实际速度降为 0，最终 `reason=11` 超时。

同一路线不同测试的倒车入口横向误差曾出现约 `+17 cm` 和 `-15 cm`，入口航向相差约 `8°`。入口姿态差异会平移、旋转整条实录倒车路线，因此入口姿态稳定性仍然是倒车重复精度的重要因素。

前进速度数据曾显示加速阶段请求速度与命令速度平均相差约 `0.95～1.0 m/s`，所以最终将加速度限制提高到 3.5 m/s²。

## 4. 已完成的验证

最新完整源码测试：

```powershell
python -m unittest discover -s tests -p "test_*.py" -v
```

结果：

```text
Ran 47 tests
OK
```

另外完成：

- 新增自适应前视曲线 C99 编译测试。
- 覆盖低速、1.0 m/s、正负输入和 1.20 m 封顶。
- 检查自适应前视只接入 portion1 实录倒车路径。
- 检查 `cfg=p1spd6` 和 `revLd100` 串口字段。
- `git diff --check` 通过。
- 修改文件 UTF-8 解码检查通过。
- 合并后的 `final` 再次运行 47 项测试通过。
- 推送后本地与远程 `final` SHA 一致。

注意：这些测试不能替代 ADS 编译和实车测试。

## 5. 未完成事项与优化空间

### 5.1 尚未完成 ADS 编译和烧录

当前 Codex 环境没有 TASKING `amk/cctc` 工具链，因此没有完成 TC264 固件的 Clean/Build/Link。

必须在 AURIX Development Studio 中：

1. 切换/确认工程使用本地 `kart_508`。
2. Clean Project。
3. 重新 Build。
4. 确认没有 RAM overflow、重复符号、Flash 冲突或 unresolved external。
5. 烧录后确认日志为 `cfg=p1spd6`。

### 5.2 尚未实车验证 p1spd6

以下仍需实车确认：

- `-10` 倒车时左右摆动是否明显减少。
- `revLd100` 是否在巡航时约 71、精调时约 35。
- `revCmd10` 正负切换次数和幅度是否下降。
- `actS10` 是否能及时跟随 `revCmd10`。
- 倒车最终横向、纵向和航向误差。
- 连续多次测试的重复性。

### 5.3 尚未继续调整倒车转向参数

没有降低倒车转向增益，也没有调整转向速率或底层转向 PID。

下一份日志判断规则：

- `revCmd10` 自身正负大幅摆动：继续优化上层前视/增益。
- `revCmd10` 平稳但 `actS10` 过冲摆动：再检查底层转向 PID。
- 不要在没有日志证据时同时修改前视、增益和 PID。

### 5.4 最终停车距离没有收紧

倒车结束距离仍保持约 12 cm，没有改成 8 cm。

原因：应先验证停车入口姿态和高速倒车路径稳定，再收紧最终停止距离，否则可能增加超时或撞击风险。

### 5.5 Portion3 路线长度问题没有处理

之前发现 portion3 保存后与重新上电读取后可能差一个点，疑点仍在 `Flash_Read_portion_3points()` 的：

```c
portion_3.length_index = stored_length - 1;
```

该问题本轮明确延期，没有修改 portion3，也没有做保存/断电/重启逐点一致性测试。

### 5.6 倒车入口姿态差异没有从结构上解决

停车入口减速距离已经提前到 2.0 m，但尚未通过实车证明入口横向误差和航向误差已经稳定。

实录倒车路线仍会根据运行时停车入口姿态做局部 SE(2) 对齐；如果车辆停车位置和航向每次差异很大，整条倒车路线仍可能整体偏移。

### 5.7 Portion1 曾出现少一个路线点，根因未确认

最早一份记录中曾出现车辆没有完全倒入、看起来少了一个路线点的现象，之后用户重新打点。本轮没有形成能够证明是记录、保存、读取还是结束标记导致的确定结论。

如果新路线再次出现同类问题，应同时保存：

- 打点完成时的路线长度和最后三个点。
- Flash 保存后的长度。
- 重新上电读取后的长度和最后三个点。
- 倒车时的 `revIdx/revLen`。

不要直接通过增加一个虚拟终点掩盖问题。

### 5.8 最大倒车菜单值未做安全实测

菜单允许到 `-40`，对应约 4.0 m/s，但该速度没有经过当前停车场景的实车安全验证。不要直接从低速跳到 `-40`。

## 6. 下一步实车测试顺序

一次只测试一个变量：

1. 在 ADS Clean/Build 并烧录。
2. 串口确认 `cfg=p1spd6`。
3. 先用较低倒车速度确认方向、停车和制动均正常。
4. 菜单设为 `-10`，同一路线连续测试至少 5 次。
5. 每次保存完整 JSON 日志，并记录是否正常结束、最终位置误差和航向误差。
6. 对比 `revLd100`、`revCmd10`、`actS10`、`revD100`、`revYaw10`。
7. 如果上层命令仍摆动，再单独把高速倒车增益从 1.40 小幅降低；不要同时改底层 PID。
8. 入口姿态稳定后，再决定是否把最终停止距离从 12 cm 收紧到 8 cm。
9. Portion1 稳定后，再单独处理 portion3 路线长度不一致。

## 7. 主要修改文件

| 文件 | 本轮作用 |
|---|---|
| `code/guandao.c` | portion1 速度规划、停车入口、倒车路线跟踪和自适应前视 |
| `code/guandao.h` | 倒车诊断接口 |
| `code/guandao_speed_planner.h` | 科目一弯道档位滞回和加减速限制 |
| `code/guandao_reverse_speed_planner.h` | 菜单倒车速度、精调速度和速度自适应前视 |
| `code/display.c` | 倒车菜单范围 `-2～-40` |
| `code/flash.c` | 倒车菜单默认值和 Flash 参数校验 |
| `code/rear_motor/rear_motor.c` | 倒车低速 PWM 启动下限逻辑 |
| `code/rear_motor/rear_reverse_pwm_floor.h` | 倒车启动 PWM 下限纯函数 |
| `user/cpu0_main.c` | `cfg=p1spd6` 和 portion1 串口诊断 |
| `tests/test_portion1_speed_planner.py` | 前进速度规划和串口字段测试 |
| `tests/test_portion1_menu_reverse_speed.py` | 菜单倒车速度及自适应前视测试 |
| `tests/test_rear_reverse_fine_speed.py` | 倒车精调 PWM 下限测试 |

设计和计划：

- `docs/superpowers/specs/2026-07-26-portion1-adaptive-reverse-lookahead-design.md`
- `docs/superpowers/plans/2026-07-26-portion1-adaptive-reverse-lookahead.md`

## 8. Git 与本地工作树注意事项

当前功能分支已经合入并上传 `final`。继续工作前：

```powershell
git switch final
git pull origin final
git log -1 --oneline
```

本地存在用户原有、与本轮无关的内容：

- `.cproject`
- `.settings/org.eclipse.core.resources.prefs`
- 若干附件、文档、压缩包和临时目录

这些内容均未纳入本轮功能提交，也不要擅自删除、覆盖或 `git add -A`。

后续每次代码更改要求：

1. 先写失败测试。
2. 修改一个明确变量。
3. 跑完整测试。
4. 只暂存本次相关文件。
5. 提交并上传当前目标分支。
